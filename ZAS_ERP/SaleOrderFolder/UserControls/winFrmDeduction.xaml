<Window x:Class="ZAS_ERP.SaleOrderFolder.UserControls.winFrmDeduction"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZAS_ERP.SaleOrderFolder.UserControls"
        xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
        xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
        mc:Ignorable="d"
        Title="winFrmDeduction" Height="200" Width="300" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="30"/>


            </Grid.RowDefinitions>

            <Label Content="Title:" Margin="0 0 5 0" HorizontalAlignment="Center" Grid.Row="1" Grid.Column="1" />
            <dxe:TextEdit  Grid.Row="1" Grid.Column="2"  x:Name="txtDeductionTitle"/>

            <Label Content="isActive:" Margin="0 5 0 0" HorizontalAlignment="Center" Grid.Row="4" Grid.Column="1" />

            <dxe:CheckEdit Grid.Row="4" Grid.Column="2" Margin="6 0 0 0" x:Name="chkEdtIsActive" Checked="ChkEdtIsActive_Checked" Unchecked="ChkEdtIsActive_Unchecked"/>

            <Label Content="Chart of Account:" Margin="0 0 5 0" HorizontalAlignment="Center" Grid.Row="3" Grid.Column="1" />
            <dxg:LookUpEdit x:Name="lookupcoaAccounts"
                    DisplayMember="accountName"
                    Grid.Row="3"
                    Grid.Column="2"    
                    SelectedIndexChanged="CmbcoaAccounts_SelectedIndexChanged">
                <dxg:LookUpEdit.PopupContentTemplate>
                    <ControlTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>

                            <dxg:GridControl Name="PART_GridControl"
                             AutoPopulateColumns="True"
                             Grid.Row="1">
                                <dxg:GridControl.View>
                                    <dxg:TreeListView Name="treeListView1"
                                  ShowAutoFilterRow="True"
                                  TreeDerivationMode="Selfreference"
                                  KeyFieldName="Id"
                                  ParentFieldName="parentId"
                                  />
                                </dxg:GridControl.View>
                                <dxg:GridColumn FieldName="Account Name"
                              IsSmart="True"
                              Width="auto"
                              Visible="False"/>
                            </dxg:GridControl>
                        </Grid>
                    </ControlTemplate>
                </dxg:LookUpEdit.PopupContentTemplate>
            </dxg:LookUpEdit>


        </Grid>
        <Button Grid.Row="1" Content="Save" Click="Button_Click" />
    </Grid>
    
   
</Window>
