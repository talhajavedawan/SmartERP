<UserControl x:Class="ZAS_ERP.SaleOrderFolder.UserControls.ucSaleReceiptsStatusList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZAS_ERP.SaleOrderFolder.UserControls"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:GetAllStatuses/>
    </UserControl.DataContext>
    <Grid>
        <dxg:GridControl x:Name="grdStatus"
                       MaxHeight="1300"
                       AutoGenerateColumns="AddNew"
                       Grid.Row="1"
                       SelectionMode="Row" 
                         ItemsSource="{Binding StatusList}"
                         SelectionChanged="GrdStatus_SelectionChanged" >
            <dxg:GridControl.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Add New Sale Receipt Status"
                      x:Name="btnNewSaleReceiptStatus"
                      Click="BtnNewSaleReceiptStatus_Click">
                        <MenuItem.Icon>
                            <Image Source="/ZAS_ERP;component/images/icon-Add-ReportGroups.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Edit Sale Receipt Status"
                      x:Name="btnEditSaleReceiptStatus"
                      Click="BtnEditSaleReceiptStatus_Click">
                        <MenuItem.Icon>
                            <Image Source="/ZAS_ERP;component/images/icon-edit-ReportGroups.png"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <!--<Separator />
            <MenuItem Header="Delete SaleInvoice Status"
                      x:Name="btnDeleteSaleInvoiceStatus" />-->
                </ContextMenu>
            </dxg:GridControl.ContextMenu>
        </dxg:GridControl>
    </Grid>
</UserControl>
