<UserControl x:Class="ZAS_ERP.ToDoTasks.UserControls.ucFrmStatusCalculationType"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZAS_ERP.ToDoTasks.UserControls"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="800" Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <dx:DXTabControl>
            <dx:DXTabItem Header="Status Calculation Type">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Calculation Type:  " HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    <dxe:TextEdit Grid.Row="0" Grid.Column="1" Height="22" Margin="20 0 20 0" x:Name="txtCalculationType"/>

                    <TextBlock Text="Achieved Field:  " HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="1"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" Height="22" Margin="20 0 20 0" x:Name="cmbxAchievedPoints">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel  Orientation="Horizontal">
                                    <TextBlock Text="{Binding id}"
                               Visibility="Hidden" />
                                    <TextBlock Text="{Binding name}"></TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <TextBlock Text="Total Field:  " HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="2"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" Height="22" Margin="20 0 20 0" x:Name="cmbxTotalPoints">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel  Orientation="Horizontal">
                                    <TextBlock Text="{Binding id}"
                               Visibility="Hidden" />
                                    <TextBlock Text="{Binding name}"></TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                </Grid>
            </dx:DXTabItem>
            
        </dx:DXTabControl>

        <dx:SimpleButton Grid.Row="1" Height="30" Content="Save" FontWeight="Bold" FontSize="16" x:Name="btnSave" Click="BtnSave_Click"/>
    </Grid>
</UserControl>
