<Application x:Class="ZAS_ERP.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ZAS_ERP"
             StartupUri="start/frmserverConnect.xaml"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxnav="http://schemas.devexpress.com/winfx/2008/xaml/navigation">
  <!--StartupUri="start/frmserverConnect.xaml"Customerss/frmCustomerCenter.xaml-->
    
  <Application.Resources>

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ToastNotifications.Messages;component/Themes/Default.xaml" />
                <ResourceDictionary Source="/ZAS_ERP;component/Resources/CompactMode/CompactModeTemplates.xaml" />

            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="ToggleButton" TargetType="{x:Type ToggleButton}">
                <Setter Property="Cursor"  Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                            <Border CornerRadius="15" Background="{TemplateBinding Background}"
                                BorderThickness="1"    Opacity="{TemplateBinding OpacityMask}">

                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger  Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="1"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value=".3"/>

                                </Trigger>
                            </ControlTemplate.Triggers>

                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <DataTemplate x:Key="reportHeaderTemplate">
            <Border HorizontalAlignment="Stretch" Margin="0,0,0,10" BorderBrush="Gray" BorderThickness="1">
                <TextBlock Text="Header" Foreground="Black" FontSize="20"  FontWeight="Bold" />
            </Border>
        </DataTemplate>
    <!--<Style TargetType="dxlc:GroupBox"
          
           BasedOn="{StaticResource {x:Type dxlc:GroupBox}}">
      <Setter Property="Background" Value="White"></Setter>
      <Setter Property="dxlc:GroupBox.TitleBackground">
        <Setter.Value>
          <LinearGradientBrush EndPoint="0.5,1"
                               StartPoint="0.5,0">
            <GradientStop Color="#FFE6E8EB"
                          Offset="0" />
            <GradientStop Color="#FF6CA6FB"
                          Offset="1" />
          </LinearGradientBrush>
        </Setter.Value>
      </Setter>
    </Style>-->
      <!--Alternate Row Background For TableView In GridControl -->
      
      <!--<Style TargetType="dxg:GridControl"
             BasedOn="{StaticResource {x:Type dxg:GridControl}}">
        <Setter Property="dx:DXSerializer.StoreLayoutMode"
                Value="All"></Setter>
        <Setter Property="dxg:GridSerializationOptions.AddNewColumns"
                Value="True"></Setter>
        <Setter Property="dxg:GridSerializationOptions.RemoveOldColumns"
                Value="False" />
        
      </Style>-->

            <!--<Style TargetType="dxnav:TileNavPane"
             BasedOn="{StaticResource {x:Type dxnav:TileNavPane}}">
                <Setter Property="BorderBrush"
                Value="#FF002256"></Setter>
                <Setter Property="BorderThickness"
                Value="2"></Setter>
                <Setter Property="ShowItemShadow"
                Value="True"></Setter>

                <Setter Property="CloseOnOuterClick"
                Value="True"></Setter>
                <Setter Property="FlyoutShowMode"
                Value="Adorner"></Setter>
                <Setter Property="OpacityMask"
                Value="Red"></Setter>
                <Setter Property="Opacity"
                Value="0.7"></Setter>
                <Setter Property="AllowDrop"
                Value="True"></Setter>
                <Setter Property="CloseOnOuterClick"
                Value="True"></Setter>
                <Setter Property="ShowItemShadow"
                Value="True"></Setter>
                <Setter Property="dxnav:TileNavPane.Background">
                    <Setter.Value>
                        <LinearGradientBrush EndPoint="0.5,1"
                               StartPoint="0.5,0">
                            <GradientStop Color="#FFCBD8EC"
                          Offset="0" />
                            <GradientStop Color="#FF0B49A5"
                          Offset="1" />
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
             
            </Style>
            <Style TargetType="dxnav:TileNavCategory"
             BasedOn="{StaticResource {x:Type dxnav:TileNavCategory}}">

                <Setter Property="Height"
                Value="60"></Setter>
                <Setter Property="Width"
                Value="130"></Setter>
                <Setter Property="Cursor"
                Value="Hand"></Setter>
                <Setter Property="AllowDrop"
                Value="True"></Setter>
                
                
                

            </Style>-->
            

            <!--Alternate Row Background For TableView In GridControl -->

      <!--<Style TargetType="dxg:TableView"
             BasedOn="{StaticResource {x:Type dxg:TableView}}">
        <Setter Property="AlternateRowBackground"
                Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"></Setter>
        <Setter Property="ColumnFilterPopupMode"
                Value=" Excel"></Setter>
        <Setter Property="AllowCascadeUpdate"
                Value="True" />
      </Style>--> 
      <!--Alternate Row Background For TreeListView In GridControl -->
      <!--<Style TargetType="dxg:TreeListView"
             BasedOn="{StaticResource {x:Type dxg:TreeListView}}">
        <Setter Property="AlternateRowBackground"
                Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"></Setter>
        <Setter Property="ColumnFilterPopupMode"
                Value=" Excel"></Setter>
      </Style>-->
            <Style TargetType="ComboBox" x:Key="cmbItemTemplate">
                <Setter Property="ItemTemplate">
                    <Setter.Value>
                        
                            <DataTemplate>
                                <StackPanel  Orientation="Horizontal">
                                    <TextBlock Text="{Binding id}"
                             Visibility="Hidden" />
                                    <TextBlock Text="{Binding name}"></TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        
                    </Setter.Value>
                </Setter>
                
            </Style>
            
          
                <!--button style for roundCornerButton(waqas)-->
            <Style x:Key="RoundButtonTemplate"  TargetType="Button">
              
                <!--<Setter Property="Background" Value="White"/> 
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="Opacity" Value="0.5"/> 
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="9" Background="{TemplateBinding Background}" BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                                </ContentPresenter>
                            </Border>
                        </ControlTemplate>     
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#05628E"/>
                        <Setter Property="Opacity" Value="0.8"/> 

                    </Trigger>
                </Style.Triggers>-->
            </Style>

            <Style x:Key="Simplebutton"  TargetType="dx:SimpleButton">
                <!--<Setter Property="Background" Value="AliceBlue"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="dx:SimpleButton">
                            <Border CornerRadius="5" Background="{TemplateBinding Background}" BorderThickness="1" BorderBrush="Black">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
                                </ContentPresenter>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                    
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="LightBlue"/>
                    </Trigger>
                </Style.Triggers>-->
            </Style>



            <!--<ControlTemplate x:Key="errorTemplate">
      <Border BorderBrush="OrangeRed"
              BorderThickness="2">
        <Grid>
          <AdornedElementPlaceholder />
          <TextBlock Text="xxxx"
                     Foreground="OrangeRed"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Right"
                     Margin="0,0,4,0" />
        </Grid>
      </Border>
    </ControlTemplate>-->
            <!--<Style x:Key="{x:Type TreeView}" 
       TargetType="TreeView" >
                <Setter Property="ItemTemplate" >
                    <Setter.Value>
                        <HierarchicalDataTemplate DataType="{x:Type local:cmbitem}" ItemsSource="{Binding Items}" >
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />

                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="1*" />

                                </Grid.ColumnDefinitions>
                                -->
            <!--<CheckBox IsChecked="{Binding isActive}" IsEnabled="False"/>--><!--
                                <TextBlock Text="{Binding id}"
                         Visibility="Collapsed" />
                                <TextBlock Text="{Binding description}"
                         Visibility="Collapsed" />
                                <TextBlock Text="   "
                         Background="{Binding bcolor}"
                         Width="20"
                         Margin="0,0,5,0" Grid.Column="1"/>
                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                    <TextBlock Text="{Binding name}"
                         Grid.Column="1"
                         
                         HorizontalAlignment="Stretch" />

                                    --><!--<TextBlock Text=" [" Foreground="Blue" />--><!--
                                    <Label Content="{Binding fcolor}" Foreground="Blue" MouseDoubleClick="Label_MouseDoubleClick" />
                                    --><!--<TextBlock Text="]" Foreground="Blue" />--><!--
                                </StackPanel>
                            </Grid>
                        </HierarchicalDataTemplate>
                    </Setter.Value>
                </Setter>


            </Style>-->
            <!--<ControlTemplate x:Key="TextBoxErrorTemplate">
      <DockPanel LastChildFill="True">
        <TextBlock DockPanel.Dock="Right"
                   Foreground="Orange"
                   FontSize="12pt">!!!!</TextBlock>
        <Border BorderBrush="Red"
                BorderThickness="1">
          <AdornedElementPlaceholder />
        </Border>
      </DockPanel>
    </ControlTemplate>-->
    <!--Message box validation template-->
    <!--<Style TargetType="{x:Type TextBox}" >
      <Setter Property="Validation.ErrorTemplate">
        <Setter.Value>
          <ControlTemplate>
            <DockPanel LastChildFill="True">
              <TextBlock DockPanel.Dock="Bottom"
                         Foreground="Red"
                         FontSize="8pt"
                         Text="{Binding ElementName=MyAdorner, 
                               Path=AdornedElement.(Validation.Errors)[0].ErrorContent}"></TextBlock>
              <Border BorderBrush="Red"
                      BorderThickness="2">
                <AdornedElementPlaceholder Name="MyAdorner" />
              </Border>
            </DockPanel>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>-->
      <Style TargetType="Window">
        
        <Setter Property="Icon"
                Value="/ZAS_ERP;component/images/zasLogo.png" />
      </Style>
      <Style TargetType="Window"
           x:Key="bluewindowstyle">
      <Setter Property="Background">
        <Setter.Value>
          <LinearGradientBrush EndPoint="0.5,1"
                               StartPoint="0.5,0">
            <GradientStop Color="#FF5585EC"
                          Offset="0" />
            <GradientStop Color="#FFB2CAFF"
                          Offset="1" />
            <GradientStop Color="#FFDEE8FF"
                          Offset="1" />
            <GradientStop Color="#FFDFE9FF"
                          Offset="1" />
            <GradientStop Color="White"
                          Offset="0.85" />
            <GradientStop Color="White"
                          Offset="0.16" />
          </LinearGradientBrush>
        </Setter.Value>
      </Setter>
        <Setter Property="Icon"
                Value="images/zasLogo.png" />
      </Style>
    <!--<DataTemplate x:Key="checkedlistbox">
      <StackPanel Orientation="Horizontal">
        <CheckBox Margin="10,0"
                  Content=""
                  x:Name="chklistsection"></CheckBox>
        <TextBlock></TextBlock>
      </StackPanel>
    </DataTemplate>-->
      
    </ResourceDictionary>
  </Application.Resources>
</Application>
