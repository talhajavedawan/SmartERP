<UserControl x:Class="ZAS_ERP.Chat.Usercontrols.ucRecentChats"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZAS_ERP.Chat.Usercontrols"
             mc:Ignorable="d" 
             Height="auto" Width="auto" MinWidth="248"  MouseEnter="UserControl_MouseEnter" MouseLeave="UserControl_MouseLeave" Cursor="Hand" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MouseDown="UserControl_MouseDown" >
    <Grid>
        
        <Border x:Name="brdRecents" BorderBrush="Gray" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" >
                    <Border  Padding="8" 
                BorderBrush="Transparent"  
                HorizontalAlignment="Left"    
                CornerRadius="30"
                 Background="Gray" Height="50" Width="50" Opacity="0.2" Margin="5,5,5,5">
                        <!--profile picture initials(text)-->

                    </Border>
                    <TextBlock x:Name="ucInitials" Text="{Binding initials}" 
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center" Foreground="#FF006CFF"         
                           FontSize="15"   TextAlignment="Center"    FontWeight="Bold" Visibility="Visible"  FontFamily="arialRounded"/>
                </Grid>
                <Grid Grid.Column="1" Margin="5,5,5,5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="userName" Grid.Column="0"  FontFamily="ArialRounded" FontSize="15"   Text="{Binding userName}" FontWeight="DemiBold"/>
                        <Border x:Name="brdrMsgCounter"
                                Grid.Column="1"
                                BorderThickness="2"
                                Height="20" 
                                Width="20" 
                                CornerRadius="15"
                                Background="red"
                                
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                               
                                Visibility="Collapsed" >
                            <TextBlock 
                                x:Name="msgCounter"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center" 
                                Foreground="White"
                                FontSize="10"
                                Text=""  
                                TextAlignment="Center"
                                FontWeight="bold"   />

                        </Border>
                    </Grid>
                    <TextBlock x:Name="msgBody" Grid.Row="1" Width="200" FontFamily="ArialRounded" Text="{Binding msgBody}" TextTrimming="CharacterEllipsis" Opacity="0.9" />
                    <TextBlock x:Name="seenStamp" Grid.Row="2"  FontFamily="ArialRounded" Text="{Binding seenStamp}" HorizontalAlignment="Left" Opacity="0.7"/>
                    <TextBlock x:Name="contactId"  Text="{Binding contactId}" Visibility="Hidden"/>
                    <TextBlock x:Name="receiverId" Text="{Binding receiverId}" Visibility="Hidden"/>
                    <TextBlock x:Name="senderId" Text="{Binding senderId}" Visibility="Hidden"/>
                    <TextBlock x:Name="lastMsgId" Text="{Binding msgId}" Visibility="Hidden"/>

                </Grid>
            </Grid>
        </Border>
        
    </Grid>
</UserControl>
