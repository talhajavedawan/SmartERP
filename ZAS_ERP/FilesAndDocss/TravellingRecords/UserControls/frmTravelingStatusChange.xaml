<UserControl x:Class="ZAS_ERP.FilesAndDocss.TravellingRecords.UserControls.frmTravelingStatusChange"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZAS_ERP.FilesAndDocss.TravellingRecords.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="450" Loaded="UserControl_Loaded">
    <Grid Background="Gray">
        <Grid Height="240" Width="380" Background="White">

            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition/>
                <RowDefinition Height="35"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Current Status" Grid.Column="0" Grid.Row="0" FontSize="18" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            <TextBlock Text="   " x:Name="statusColor"  Width="35" Grid.Column="1" Grid.Row="0" FontSize="18" VerticalAlignment="Center"/>
            <TextBlock x:Name="statusName" Text="" Grid.Column="2" Grid.Row="0" FontSize="18" VerticalAlignment="Center"/>

            <TextBlock Text="Change to" Grid.Column="0" Grid.Row="1" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            <ComboBox Grid.Row="1" VerticalAlignment="Center" Width="200"  Grid.ColumnSpan="2" Grid.Column="1"
                                        TabIndex="11" x:Name="cmbBillStatus">
                <ComboBox.ItemTemplate>

                    <DataTemplate>
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"></ColumnDefinition>
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding id}" Visibility="Hidden" />
                            <TextBlock Text="   " Background="{Binding bcolor}"
                                                Width="20" Margin="10,0" />
                            <TextBlock Text="{Binding name}" Grid.Column="1"
                                                Foreground="{Binding fcolor}"
                                                HorizontalAlignment="Stretch"/>
                        </Grid>
                    </DataTemplate>
                </ComboBox.ItemTemplate>


            </ComboBox>

            <Button x:Name="btnSave" Content="save" Grid.Row="2" Grid.ColumnSpan="3" Click="Button_Click"/>
        </Grid>
    </Grid>
</UserControl>
