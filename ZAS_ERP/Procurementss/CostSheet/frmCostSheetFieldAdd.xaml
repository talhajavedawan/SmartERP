<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:ZAS_ERP.Procurementss"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
        xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
        x:Class="ZAS_ERP.Procurementss.frmCostSheetFieldAdd"
             mc:Ignorable="d"
        xmlns:utilities="clr-namespace:ZAS_ERP.BussinessLogicss"
        utilities:WindowUtilities.CloseOnEscape="True"
        WindowStartupLocation="CenterOwner"
             Height="275"
             Width="400" Title=" Cost Sheet Field" x:Name="winCostSheetFieldAdd" 
        Loaded="winCostSheetFieldAdd_Loaded" Closing="winCostSheetFieldAdd_Closing">
  <Grid Margin="0,20,0,0">
    <Grid.Resources>
      <Style TargetType="{x:Type Label}">
        <Setter Property="Margin"
                Value="10,10" />
        <Setter Property="Foreground"
                Value="Gray" />
        <Setter Property="VerticalAlignment"
                Value="Center" />
      </Style>
      <Style TargetType="{x:Type TextBox}">
        <Setter Property="Margin"
                Value="10,0" />
        <Setter Property="MaxWidth"
                Value="350" />
      </Style>
    </Grid.Resources>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width=" auto" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height=" auto" />
      <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height=" 50" />
    </Grid.RowDefinitions>
    <Label Content="Field Title"
           ToolTip="Cost Sheet Field"
           HorizontalAlignment="Right" />
    <TextBox x:Name="txtcostSheetField"
             Grid.Column="1"
             VerticalAlignment="Center" />

        <Label Content="Hierarchical Index"
           ToolTip="Select Unique Hierarchical Index(Higher index# means higher priority level)"
           HorizontalAlignment="Right"
           Grid.Row="2" />
        <dxe:SpinEdit x:Name="spnHierarchicalIndex"
                Margin="10,0"
                MinValue="1"
                      
                      MaxValue="100"
                VerticalAlignment="Center"
                Grid.Column="1"
                Grid.Row="2" />


        <!--<Label Content="Credit Chart of Account"
           ToolTip="Credit Account"
           HorizontalAlignment="Right"
           Grid.Row="3" />
        <dxg:LookUpEdit x:Name="cmbCreditAccounts"
                    Grid.Column="1"
                                 
                                  ToolTip="Select Credit Account"
                                     Cursor="Hand"
                                    ShowNullText="True"
                                    NullText="Select Credit Account"
                    Grid.Row="3"
                                 DisplayMember="accountName"  
                    VerticalAlignment="Center"
                    SelectedIndexChanged="CmbCreditAccounts_SelectedIndexChanged"
                    Margin="15,15,15,15"
                    >
            <dxg:LookUpEdit.PopupContentTemplate>
                <ControlTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>

                        <dxg:GridControl Name="PART_GridControl"
                             AutoPopulateColumns="True"
                             Grid.Row="1">
                            <dxg:GridControl.View>
                                <dxg:TreeListView Name="treeListView1"
                                  ShowAutoFilterRow="True"
                                  TreeDerivationMode="Selfreference"
                                  KeyFieldName="Id"
                                  ParentFieldName="parentId"
                                  />
                            </dxg:GridControl.View>
                            <dxg:GridColumn FieldName="Account Name"
                              IsSmart="True"
                              Width="auto"
                              Visible="False"/>
                        </dxg:GridControl>
                    </Grid>
                </ControlTemplate>
            </dxg:LookUpEdit.PopupContentTemplate>
        </dxg:LookUpEdit>
        <Label Content="Debit Chart of Account"
           ToolTip="Debit Account"
           HorizontalAlignment="Right"
           Grid.Row="4" />
        <dxg:LookUpEdit x:Name="cmbDebitAccounts"
                    Grid.Column="1"
                                 
                                  ToolTip="Select Debit Account"
                                     Cursor="Hand"
                                    ShowNullText="True"
                                    NullText="Select Debit Account"
                    Grid.Row="4"
                    VerticalAlignment="Center"
                    DisplayMember="accountName"    
                    SelectedIndexChanged="CmbDebitAccounts_SelectedIndexChanged"
                    Margin="15,15,15,15"
                    >
            <dxg:LookUpEdit.PopupContentTemplate>
                <ControlTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>

                        <dxg:GridControl Name="PART_GridControl"
                             AutoPopulateColumns="True"
                                         
                             Grid.Row="1">
                            <dxg:GridControl.View>
                                <dxg:TreeListView Name="treeListView1"
                                  ShowAutoFilterRow="True"
                                  TreeDerivationMode="Selfreference"
                                  KeyFieldName="Id"
                                  ParentFieldName="parentId"
                                  />
                            </dxg:GridControl.View>
                            <dxg:GridColumn FieldName="Account Name"
                              IsSmart="True"
                              Width="auto"
                              Visible="False"/>
                        </dxg:GridControl>
                    </Grid>
                </ControlTemplate>
            </dxg:LookUpEdit.PopupContentTemplate>
        </dxg:LookUpEdit>-->


        <Label Content="isActive"
           ToolTip="Unselect to make it inActive"
           HorizontalAlignment="Right"
           Grid.Row="5" />
    <CheckBox x:Name="chkisactive"
                Margin="10,0"
                IsChecked="True"
              VerticalAlignment="Center"
                Grid.Column="1"
                Grid.Row="5" />
    <Label Content="Field Type"
           HorizontalAlignment="Right"
           Grid.Row="1" />
    <ComboBox x:Name="cmbType"
                    Grid.Column="1"
                    Grid.Row="1"
                    VerticalAlignment="Center"
                    Margin="10,5"
            
                    TabIndex="3">
            <ComboBoxItem Content="Dynamic" IsSelected="True"/>
            <ComboBoxItem Content="Static"/>

            <!--<dxg:LookUpEdit.PopupContentTemplate>
        <ControlTemplate>
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="auto"></RowDefinition>
              <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            --><!--<Button x:Name="btnAddCategory"
                    Click="btnAddCategory_Click"
                    VerticalAlignment="Center"
                    Content="Add New"
                    Foreground="BlueViolet" />--><!--
            <dxg:GridControl Name="PART_GridControl"
                             AutoPopulateColumns="True"
                             Grid.Row="1">
              <dxg:GridControl.View>
                <dxg:TreeListView Name="treeListView1"
                                  ShowAutoFilterRow="True"
                                  TreeDerivationMode="Selfreference"
                                  KeyFieldName="Id"
                                  ParentFieldName="parentId"
                                  />
              </dxg:GridControl.View>
              <dxg:GridColumn x:Name="colId"
                              FieldName="Id"
                              Visible="False" />
              <dxg:GridColumn FieldName="category"
                              IsSmart="True"
                              Width="auto" />
            </dxg:GridControl>
          </Grid>
        </ControlTemplate>
      </dxg:LookUpEdit.PopupContentTemplate>-->
    </ComboBox>
    <Button Grid.ColumnSpan="2"
            Grid.Row="6"
            Content="Save"
            FontSize="26"
            x:Name="btncostSheetFieldSave"
            Click="btncostSheetFieldSave_Click" />

  </Grid>
</Window>
